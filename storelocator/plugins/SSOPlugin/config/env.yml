# ------------------------------------------
# DEFAULT PER TUTTI I DOMINI NON IN ELENCO
# ------------------------------------------
all:
  signin:
    sso_cookie_name: diesel_sso 
    
#  sync:
#    legacy:
#      enabled:  true


# ------------------------------------------
# CORE.DIESEL.LOCALHOST
# ------------------------------------------
.diesel.localhost:

  # dominio del cookie di SSO
  sso_cookie_domain: .diesel.localhost

# ------------------------------------------
# CORE.DIESEL.H-ART.IT
# ------------------------------------------
.diesel.h-art.it:

  # dominio del cookie di SSO
  sso_cookie_domain: diesel.h-art.it

# ------------------------------------------
# CORE.STAGE.DIESEL.COM
# ------------------------------------------
.stage.diesel.com:

  # dominio del cookie di SSO
  sso_cookie_domain: stage.diesel.com

# ------------------------------------------
# CORE.STAGE2.DIESEL.COM
# ------------------------------------------
.stage2.diesel.com:

  # dominio del cookie di SSO
  sso_cookie_domain: stage2.diesel.com

# ------------------------------------------
# CORE.DIESEL.COM
# ------------------------------------------
.diesel.com:

  # dominio del cookie di SSO
  sso_cookie_domain: .diesel.com
